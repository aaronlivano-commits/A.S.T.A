# --- Web framework ---
fastapi
uvicorn[standard]
python-multipart
pydantic
pydantic-settings

# --- Auth (Firebase Admin) ---
firebase-admin
PyJWT
# cryptography 42+ ships rust bindings that fail to load on Python 3.9 (DLL
# load error 127). Pin to <42 until the project moves to Python 3.10+.
cryptography

# --- Google Gemini ---
google-genai

# --- Data training & parsing ---
pandas
openpyxl
numpy

# --- Vision / OCR ---
opencv-python-headless
easyocr
Pillow

# --- Vector store ---
chromadb
# faiss-cpu==1.8.0.post1  # optional alternative

# --- PEFT / LoRA fine-tuning ---
torch
peft
safetensors

# --- Utilities ---
python-dotenv
